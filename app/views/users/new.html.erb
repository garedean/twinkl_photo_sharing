<h1>Picture List</h1>

<%= form_for @user, :url => root_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :avatar %>
  <%= form.submit "Submit" %>
<% end %>

<h3>Add a photo link</h3>

<%= image_tag @user.avatar.url %>
